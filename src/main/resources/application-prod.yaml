spring:
  application:
    name: card
  datasource:
    url: jdbc:postgresql://card-postgres-card:5432/click?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: {user}
    password: {password}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

jwt:
  secret: {secret}
